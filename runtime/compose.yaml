# Generated from the compiled scenario. Do not edit by hand; change scenario/steps and reapply.
x-runtime-environment: &runtime-environment
  DEMO_GENERATION_ID: ${DEMO_GENERATION_ID:?missing demo generation}

x-rotated-logs: &rotated-logs
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

x-orders: &orders
  build: ./repos/demo-orders
  image: clean-room-demo/demo-orders:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-profile-v004: &profile-v004
  build: ./worktrees/demo-profile-v004
  image: clean-room-demo/demo-profile:v004
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-profile-v005: &profile-v005
  build: ./worktrees/demo-profile-v005
  image: clean-room-demo/demo-profile:v005
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

services:
  orders-production:
    <<: *orders
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production-emea-central-04:
    <<: *profile-v004
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
      DEMO_CLUSTER: prod-emea-central-04
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production-eu-west-01:
    <<: *profile-v004
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
      DEMO_CLUSTER: prod-eu-west-01
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production-eu-west-02:
    <<: *profile-v005
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
      DEMO_CLUSTER: prod-eu-west-02
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production-sa-east-02:
    <<: *profile-v004
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
      DEMO_CLUSTER: prod-sa-east-02
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production-us-east-02:
    <<: *profile-v004
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
      DEMO_CLUSTER: prod-us-east-02
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  orders-staging:
    <<: *orders
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_STAGING:?missing staging SDK key}
      DEMO_ENVIRONMENT: staging
    command: ["npm", "run", "traffic", "--", "--profile", "staging"]
  profile-staging:
    <<: *profile-v005
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_STAGING:?missing staging SDK key}
      DEMO_ENVIRONMENT: staging
    command: ["npm", "run", "traffic", "--", "--profile", "staging"]
