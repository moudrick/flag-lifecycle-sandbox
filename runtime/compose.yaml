# Generated from the compiled scenario. Do not edit by hand; change scenario/steps and reapply.
x-runtime-environment: &runtime-environment
  DEMO_GENERATION_ID: ${DEMO_GENERATION_ID:?missing demo generation}

x-rotated-logs: &rotated-logs
  driver: json-file
  options:
    max-size: 10m
    max-file: "3"

x-catalog: &catalog
  build: ./repos/demo-catalog
  image: clean-room-demo/demo-catalog:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-checkout: &checkout
  build: ./repos/demo-checkout
  image: clean-room-demo/demo-checkout:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-notifications: &notifications
  build: ./repos/demo-notifications
  image: clean-room-demo/demo-notifications:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-orders: &orders
  build: ./repos/demo-orders
  image: clean-room-demo/demo-orders:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-profile: &profile
  build: ./repos/demo-profile
  image: clean-room-demo/demo-profile:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-shipping: &shipping
  build: ./repos/demo-shipping
  image: clean-room-demo/demo-shipping:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

x-storefront: &storefront
  build: ./repos/demo-storefront
  image: clean-room-demo/demo-storefront:local
  init: true
  restart: unless-stopped
  stop_grace_period: 30s
  logging: *rotated-logs

services:
  catalog-production:
    <<: *catalog
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  checkout-production:
    <<: *checkout
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  notifications-production:
    <<: *notifications
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  orders-production:
    <<: *orders
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  profile-production:
    <<: *profile
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  shipping-production:
    <<: *shipping
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  storefront-production:
    <<: *storefront
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_PRODUCTION:?missing production SDK key}
      DEMO_ENVIRONMENT: production
    command: ["npm", "run", "traffic", "--", "--profile", "production"]
  orders-staging:
    <<: *orders
    environment:
      <<: *runtime-environment
      LD_EVALUATION_SDK_KEY: ${LD_EVALUATION_SDK_KEY_STAGING:?missing staging SDK key}
      DEMO_ENVIRONMENT: staging
    command: ["npm", "run", "traffic", "--", "--profile", "staging"]
